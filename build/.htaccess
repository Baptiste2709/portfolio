Options -MultiViews
RewriteEngine On

# Handle images and static files first
RewriteCond %{REQUEST_FILENAME} -f
RewriteRule ^.*$ - [L]

# Handle client routing - send all other requests to index.html
RewriteRule ^(.*)$ index.html [L] 